import React from 'react';
import styles from './styles/FiltrationItem.module.scss';
const FiltrationItem = ({title, items, multiple}) => {
    return (
        <li className={styles.filtrationItem}>

        </li>
    );
};

export default FiltrationItem;